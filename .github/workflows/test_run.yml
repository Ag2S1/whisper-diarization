name: Test Run

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * TUE'

jobs:
  check-python-style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - uses: pre-commit/action@v3.0.1

  test-run:
    needs: [check-python-style]
    strategy:
      fail-fast: false
      matrix:
        os:
        - ubuntu-latest
        - windows-latest
        - macos-latest
        
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install ffmpeg
      if: runner.os == 'Linux'
      run: sudo apt update && sudo apt install -y ffmpeg

    - name: Install ffmpeg
      if: runner.os == 'macOS'
      run: brew install ffmpeg
    
    - name: Install ffmpeg
      if: runner.os == 'Windows'
      run: choco install ffmpeg

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip uv
        uv sync --python python

    - name: Test with sortformer diarizer
      env:
        HF_HUB_DISABLE_XET: "1"
        HF_HUB_ENABLE_HF_TRANSFER: "0"
      run: uv run --python python python diarize.py -a "./tests/assets/test.opus" --whisper-model tiny.en --diarizer sortformer

    - name: Test with msdd diarizer
      env:
        HF_HUB_DISABLE_XET: "1"
        HF_HUB_ENABLE_HF_TRANSFER: "0"
      run: uv run --python python python diarize.py -a "./tests/assets/test.opus" --whisper-model tiny.en --diarizer msdd
